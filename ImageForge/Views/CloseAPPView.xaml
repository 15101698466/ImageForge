<UserControl x:Class="ImageForge.Views.CloseAPPView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageForge.Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="450">
    <Border Background="White" CornerRadius="5">
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="2" Text="{Binding StrTitle}"  Style="{StaticResource  BottomText}" HorizontalAlignment="Center" FontSize="24" Height="30"/>
            <Button  Grid.Row="1" Command="{Binding YesCommand}" Style="{StaticResource btnStyle}"  HorizontalAlignment="Center">  确定</Button>
            <Button  Grid.Row="1" Command="{Binding CancelCommnd}" Grid.Column="1" Style="{StaticResource btnStyle}"  HorizontalAlignment="Center">取消</Button>

    </Grid>
    </Border>
</UserControl>
