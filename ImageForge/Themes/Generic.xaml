<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ImageForge.CustomControl">


    <Style TargetType="{x:Type local:CircularProgressBar}">
        <Setter Property="Stroke" Value="#007ACC"/>
        <Setter Property="BackgroundStroke" Value="#E0E0E0"/>
        <Setter Property="StrokeThickness" Value="12"/>
        <Setter Property="ShowPercentage" Value="True"/>
        <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self}, Path=Stroke}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CircularProgressBar}">
                    <Grid>
                       
                        <Ellipse
                            Stroke="{TemplateBinding BackgroundStroke}"
                            StrokeThickness="{TemplateBinding StrokeThickness}"
                            StrokeLineJoin="Round"
                            Fill="Transparent"/>

                       
                        <Path x:Name="PART_Arc"
                              Stroke="{TemplateBinding Stroke}"
                              StrokeThickness="{TemplateBinding StrokeThickness}"
                              StrokeLineJoin="Round"
                              Fill="Transparent"/>

                       
                        <TextBlock x:Name="PART_PercentageText"
                                   Text="{Binding RelativeSource={RelativeSource TemplatedParent},
                                                  Path=TextValue}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}"
                                   FontWeight="{TemplateBinding FontWeight}">
                            <TextBlock.Visibility>
                                <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ShowPercentage">
                                    <Binding.Converter>
                                        <BooleanToVisibilityConverter/>
                                    </Binding.Converter>
                                </Binding>
                            </TextBlock.Visibility>
                        </TextBlock>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
